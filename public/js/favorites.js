(() => { document.querySelectorAll('.js-favorite-btn').forEach((button)=>button.addEventListener('click',async()=>{const response=await fetch('/api/favorites',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':button.dataset.csrf},body:JSON.stringify({tmdbId:Number(button.dataset.tmdbId),mediaType:button.dataset.mediaType})});if(response.ok){button.textContent='In Watchlist';button.disabled=true;}else if(response.status===401){location.href='/login';}})); })();
